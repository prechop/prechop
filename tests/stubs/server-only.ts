// `server-only` throws when imported outside a React Server Component graph.
// Unit tests run under plain node, so vitest aliases the package to this no-op
// stub (see vitest.config.ts).
export {};
